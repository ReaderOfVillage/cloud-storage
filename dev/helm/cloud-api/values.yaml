replicaCount: 1
image:
  repository: ghcr.io/readerofvillage/cloud-api
  tag: 652e26bf9b17bc8a3b69a515fcce6ffdbe5624c7
secretName: cloud-api-secret
secretNameS3: s3-secret
containerPort: 8000
service:
  port: 8000
  targetPort: 8000
  type: ClusterIP
envFrom:
  - secretRef:
      name: cloud-api-secret
  - secretRef:
      name: s3-secret
volumes:
  - name: tls
    secret:
      secretName: cloud-tls
  - name: config
    configMap:
      name: app-config
  - name: postgres-data
    persistentVolumeClaim:
      claimName: postgres-pvc
ingress:
  enabled: true
  className: nginx
  host: api.dev.internal
  tls:
    enabled: false
  path: /
