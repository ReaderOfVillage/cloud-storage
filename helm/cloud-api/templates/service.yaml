apiVersion: v1
kind: Service
metadata:
  name: cloud-api
spec:
  type: {{ .Values.service.type }}

  selector:
    app: cloud-api

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}