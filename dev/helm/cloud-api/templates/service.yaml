apiVersion: v1
kind: Service
metadata:
  name: cloud-api
  labels:
    app: cloud-api
spec:
  type: {{ .Values.service.type }}

  selector:
    app: cloud-api

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}